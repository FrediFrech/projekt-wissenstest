# dao/JdbcQuestionDao.java (veraltet)

Einfache Erklärung: Diese Datei ist nur ein alter Name und zeigt auf die neue Implementierung. In neuen Teilen solltest du `JdbcQuestionRepository` verwenden.

## Zweck
Alter Klassenname für den JDBC‑Zugriff auf Fragen. Der neue Name ist `JdbcQuestionRepository`.

## Inhalt & Verantwortung
- CRUD für `questions`.
- Randomisierte Auswahl per Schwierigkeit.

## Verbindungen
- Alias für `JdbcQuestionRepository`.
